title: break
type: object
description: The BREAK resource represents a broken object. You need to create one if you want to create a broken object ARTICLE then.
x-examples:
  Example:
    type: break
    id: 42
    attributes:
      comment: The window is broken and needs a replacement
    relationships: []
    links:
      self: 'http://api.restorit.fr/breaks/42'
properties:
  type:
    type: string
    description: The type of the resource
    example: break
  id:
    type: integer
    description: The ID of the resource
    example: '42'
  attributes:
    type: object
    required:
      - comment
    properties:
      comment:
        type: string
        description: A comment giving the details of the breakage
        example: The window is broken and needs a replacement
        minLength: 1
        maxLength: 2048
  relationships:
    type: object
required:
  - type
  - id
  - attributes
x-tags:
  - Break
